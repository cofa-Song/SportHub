# Plan: Optimizing Currency Display and Formatting

Improve the readability of wallet balances by adding thousands separators and using full currency names instead of abbreviations.

## Proposed Changes

### UI Layer
#### [MODIFY] [PlayerDetail.vue](file:///c:/Users/Administrator/Desktop/營運後台/src/views/Master/PlayerDetail.vue)
- Implement a `formatNumber` utility (using `toLocaleString()` or a custom regex).
- Update `NStatistic` components:
    - Pass formatted value to `:value`. (Note: `NStatistic` might handle formatting via `separator`, but we'll ensure consistency).
    - Replace suffix `G` with [t('common.gold')](file:///c:/Users/Administrator/Desktop/%E7%87%9F%E9%81%8B%E5%BE%8C%E5%8F%B0/src/api/auth.ts#82-85).
    - Replace suffix `S` with [t('common.silver')](file:///c:/Users/Administrator/Desktop/%E7%87%9F%E9%81%8B%E5%BE%8C%E5%8F%B0/src/api/auth.ts#82-85).
    - Replace suffix `C` with [t('common.copper')](file:///c:/Users/Administrator/Desktop/%E7%87%9F%E9%81%8B%E5%BE%8C%E5%8F%B0/src/api/auth.ts#82-85).
- Ensure other areas (lists, rollover monitor) also use full names.

### Localization
#### [MODIFY] [index.ts](file:///c:/Users/Administrator/Desktop/營運後台/src/locales/index.ts)
- Add/Update keys:
    - `common.gold`: '金幣' / '金币' / 'Gold'
    - `common.silver`: '銀幣' / '银币' / 'Silver'
    - `common.copper`: '銅幣' / '铜币' / 'Copper'

## Verification Plan
1. Check wallet cards for thousands separators (e.g., `1,234,567`).
2. Verify currency names are shown in full (e.g., `金幣` instead of `G`).
3. Switch languages to ensure full names translate correctly.
