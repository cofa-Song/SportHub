# 工作報告：導覽架構調整與首頁重新配置完成

我已根據您的要求，對系統的菜單與進入點進行了簡化與優化。

## 變更內容

### 導覽菜單重構
- **更名與定位**: 將原本的「儀表板」更名為 **「首頁」 (Home)**。
- **功能移除**: 徹底從選單與路由中移除了 **「商戶管理」** 模組。
- **去冗餘**: 由於「首頁」現在直接指向個人帳號管理，我移除了原本位於「權限管理」分組下的重複「個人帳號管理」入口，使介面更加清爽。

### 首頁功能分配
- **進入點轉向**: 現在點選「首頁」或系統自動登入後進入 `/admin` 時，將會直接呈現 **「個人帳號管理」** 的內容，符合您「暫時將首頁訂為個人帳號管理」的需求。
- **全域性生效**: 此變更已同步更新至繁體中文、簡體中文與英文語系檔案。

### 路由優化
- 設置了 `/admin` 根路徑的自動跳轉，確保使用者登入後能直接到達正確的首頁。
- 清理了相關代碼中不再使用的圖標引用 (BusinessOutline, PersonCircleOutline)。

## 驗證結果
- 登入後，畫面直接顯示個人帳號管理。
- 左側菜單第一項已顯示為「首頁」，且不見「商戶管理」。
- 「權限管理」分類下不再顯示重複的個人帳號入口。

---
> [!TIP]
> 這樣的調整讓管理員在登入後能第一时间確認並維護自己的帳號資訊，提升了操作便利性。
