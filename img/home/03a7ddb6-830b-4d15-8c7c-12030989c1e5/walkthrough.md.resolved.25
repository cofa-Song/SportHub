# 工作報告：優化搜尋條件與多語系

在「所屬」搜尋功能基礎上，我進一步擴充了「註冊IP」與「註冊時間」的篩選功能，並持續優化多語系支援。

## 變更內容

### 1. 新增「註冊 IP」搜尋
- **功能**: 可針對玩家註冊時使用的 IP 位址進行模糊匹配查詢。
- **介面**: 位於搜尋列後方，新增 IP 輸入框。

### 2. 新增「註冊時間」區間選擇
- **功能**: 支援自定義日期範圍篩選，可精確找出特定時段內註冊的玩家。
- **介面**: 使用 `NDatePicker` 的日期區間模式（Date Range），操作直觀且方便。

### 3. 多語系完整覆蓋
- **內容**: 補齊了「註冊 IP」與「註冊時間」在繁中、簡中、英文環境下的翻譯，並解決了部分介面 Key 值顯示的問題。

## 驗證方法
1. **IP 搜尋**: 輸入部分 IP 段（如 `192.168`），確認能否過濾出匹配玩家。
2. **日期篩選**: 選擇註冊日期區間，確認列表僅顯示該時段內的結果。
3. **語系切換**: 確認在切換語言後，新增的 IP 與日期標籤均正確顯示。
