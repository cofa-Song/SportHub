# 工作報告：日誌系統整併與顯示優化

我已根據您的要求，將異動日誌進行了整併，並提升了資訊的可讀性。

## 變更內容

### 日誌功能整併
- **移除冗餘**: 從「玩家管理」選單中移除了「玩家操作日誌」。
- **統一入口**: 現在所有管理員在後台的操作行為（包含對玩家的異動）都統一記錄在 **「權限管理 > 後台操作日誌」**。
- **路由清理**: 刪除了 `/admin/player-logs` 路由及相關組件引用，簡化專案結構。

### 顯示資訊優化
- **功能名稱轉換**: 在日誌列表中，將原本難以理解的功能編號（例如 `OPE-103`、`OPS-601`）直接替換為對應的 **功能名稱**（例如 `帳號管理`、`玩家管理`）。
- **詳情呈現**: 在查看操作詳情時，同步將「操作模組」改為「操作功能」，並顯示易讀的名稱。

### 修復與清理
- 修正了導覽選單因整併產生的語法錯誤。
- 移除了未使用的 `DocumentTextOutline` 圖標引用，保持代碼簡潔。

## 驗證結果
- 打開「後台操作日誌」後，「功能功能」欄位已不再顯示編號，而是顯示如「玩家管理」、「群組與帳號管理」等名稱。
- 導覽菜單結構完整，切換功能正常。

---
> [!NOTE]
> 此項整併有助於管理員在單一介面審計所有操作行為，避免在多個頁面間切換。
